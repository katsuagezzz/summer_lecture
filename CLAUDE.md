# summer_lecture — 講義資料ポータル（公開リポジトリ）

iU 情報経営イノベーション専門職大学・志村ゼミの講義資料を GitHub Pages で配信する。
**受講者が実際に開くURLがここにある。**

> **このリポジトリに置いてはいけないもの**
> 学生の実名、教室での発言、文字起こし、講師版の「答え」。
> Pages で誰でも読める。これらは private リポジトリ `summer_lecture_private` 側にだけ置く。

## 3つのトラック

1つのリポジトリで3つの授業をまかなっている。**フォルダは分けない。**
3トラックで基礎知識を共有するのが目的であり、受講者から見た分離は
ポータルページのURLで完成しているため、フォルダを割っても得るものがない。

| トラック | 日時 | 人数 | 資料の狙い | ポータル | ファイル名 |
|---|---|---|---|---|---|
| **ゼミ**（メイン） | 毎週火 16:20–18:20（夏期集中も同枠） | 10名程度 | 時事とマクロ。ソクラテス式の問答で進む | `seminar.html` | `lectures/lecture_YYYYMMDD.html` |
| **佐々木さん**（個別） | 週2回・各30分（曜日は固定でない） | 1名 | **基礎知識の定着を最優先。**ゼミより一段手前から積む | `sasaki.html` | `lectures/lecture_sasaki_YYYYMMDD.html` |
| **豊長さん**（個別） | 毎週金 15:00・30分 | 1名 | **答えを載せず、空欄と問いだけを配る** | `toyonaga.html` | `lectures/lecture_toyonaga_YYYYMMDD.html` |

ゼミ資料だけトラック接頭辞が付かない（`lecture_20260825.html`）。
これは初期からの慣習で、**配布済みURLが壊れるのでリネームしない。**

`reviews/`（復習ページ）と `news_briefs/`（講義ネタ速報）は**ゼミトラック専用**。
個別指導トラックには作らない。

### ネタをどのトラックに置くか

- マクロ・時事・地政学・複数人での討議が要るもの → ゼミ
- 決算書の読み方そのもの、用語の所在、基礎の反復 → 佐々木さん
- 1つの論点を30分で深く1本 → 豊長さん

判断に迷ったら**置いてから移すのではなく、先に決める。**
過去に「ベッセント発言の4つの問い」をゼミ第3回に入れて revert し、
豊長さん 8/21 へ移設した往復がある（`d9ffa9a` 追加 → `1339308` revert → `9ccfe51` 移設）。

## ページの制約

- **単一HTMLファイル。外部依存なし。** CSSはインライン、JSは原則なし、CDNもフォントの外部読込もしない
- ライト／ダーク両対応（`@media (prefers-color-scheme: dark)` で CSS変数を差し替える）
- 印刷可
- **localStorage 等のブラウザストレージは使わない**
- 既存ページの CSS変数の意味づけを踏襲する（`--macro` マクロ／`--corp` 企業／`--bridge` 橋渡し／`--word` 用語／`--q-bg` 学生への問い）
- 新規ページは、同じトラックの直近の資料をコピーして中身を差し替えるのが速い

## 恒久の編集ルール

過去に何度も同じ直しが入っている。最初からこれで書くこと。

1. **タイトルは、扱う論点が題名で分かる形にする。**「第4回」だけ、テーマ名だけは不可
2. **宿題は1行の指示に絞る。**手順を段組みで書かない
3. **「当日の役割」のようなセクションは入れない**
4. **豊長さんトラックは答えを書かない。**空欄と問いだけ。解説を書いた時点で授業が死ぬ
5. 問いには必ず着地点（どの回のどの概念に着地させるか）を持たせる

> 8/19、第4回の資料1本に対して「3本立てに作り直し」「役割セクション削除」
> 「タイトル変更」×2「宿題を1行に」と6コミット連続した。上の5点はその結論。

## 数字の作法

判断基準の原典は private 側の `references/shimura-judgment-criteria.md` §4。要点：

- **予想／推計／報道ベース／一次情報を必ず区別してラベルする**
- 一次情報の在り処を書く（財務省・内閣府・日銀・SEC EDGAR・EDINET・各社IR）
- **率を出したら分母を書く。絶対額を出したら時点を書く**
- 率と絶対額を混ぜない
- 推計値・未確定値はページのフッターに明記する
- 講義中に口頭で示された概算・例示は、復習ページでその旨をタグで示す
- **丸めは粗めに。**`2.35ポイント` → `約2.4ポイント`、`3分の1` → `3〜4割`、`2年半` → `2年あまり`
  （見せかけの精度を出さない。`3ffc27f` の修正がこの型）
- 受講者のポートフォリオ・保有銘柄は載せない

## 1本追加・変更したときの波及先

ページは同じトラック内だけで相互リンクしている。**1本足すと、資料本体のほかに4か所を直す。**

1. `lectures/lecture_<track>_YYYYMMDD.html` を追加
2. **直前の回のHTMLの「次回」リンク**を新しい回に向ける（相互リンクなので両方向）
3. 該当トラックのポータル（`seminar.html` / `sasaki.html` / `toyonaga.html`）に行を足す
4. `README.md` の該当トラックの表に行を足す
5. private 側に講師版ノートを作る（`summer_lecture_private/CLAUDE.md` の「講師版の3点セット」）
6. `index.html` は原則さわらない（トラック振り分けのみ。新トラック追加時だけ）

**ポータルは自分のトラックの資料だけをリンクする。**
佐々木君に `sasaki.html` を渡せば他トラックが見えない、という状態を壊さないこと。

### 日程が動いたとき

**ファイル名の日付＝講義日。**日程が変わったら `git mv` でリネームし、
`<title>`・ポータル・README・相互リンクを全部追う。
過去に `lecture_sasaki_20260824.html` → `20260820.html` のリネームが発生している（`b869a04`）。
まだ配布していない資料のみ。**配布後のURLは変えない。**

## private 側の参照ファイル

資料を作る前に読むもの。実体は `summer_lecture_private`（要 clone）。

| ファイル | 何のために読むか |
|---|---|
| `skills/lecture-news-brief/references/seminar-context.md` | **まずこれ。**3トラック全部の過去回・論点・学生の到達度・チーム編成 |
| `teaching_notes/` | 該当トラックの直近回の講師版ノート。前回どこで詰まったか |
| `references/shimura-judgment-criteria.md` | 資料をレビューするとき。レッドライン8項目と採点尺度 |
| `transcripts_txt/` | 特定の発言を裏取りするときだけ grep する。**通読しない**（19本・約50万字） |

## GitHub Pages

`main` の `/(root)` を公開。`index.html` が3トラックへの振り分けハブ。
公開URL： https://katsuagezzz.github.io/summer_lecture/
